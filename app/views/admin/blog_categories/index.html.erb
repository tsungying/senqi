<% breadcrumb :blog %>

<section class="content">

  <div class="row">
    <div class="col-xs-12">
      <div class="box">
      
        <div class="box-header with-border">
          <%= link_to new_admin_blog_category_path, class: "btn btn-danger btn-sm" do %>
            <li class="fa fa-fw fa-plus-square"></li> 建立新分類
          <% end %>
        </div>

        <div class="box-body table-responsive no-padding">
          <table class="table table-hover">
            <tr>
              <th>ID</th>
              <th>分類名稱</th>
              <th>語言</th>
              <th>文章數</th>
              <th>更新時間</th>
              <th>建立時間</th>
              <th>管理</th>
            </tr>

            <% @categories.each do |category| %>
            <tr>
              <td><%= category.id %></td>
              <td><%= link_to category.name, admin_blog_category_path(category) %></td>
              <td><%= translate_language_name(category.language) %></td>
              <td><%= category.articles_count %></td>
              <td><%= l(category.updated_at, format: :long) %></td>
              <td><%= l(category.created_at, format: :long) %></td>              
              <td>
                <%= link_to "修改名稱", edit_admin_blog_category_path(category), class: "btn btn-primary btn-xs" %>
                <%= link_to "刪除此分類", [:admin, category], method: :delete, data: { confirm: "刪除此分類會同時刪除該分類底下的所有文章喔！" }, class: "btn btn-warning btn-xs" %>
              </td>
            </tr>
            <% end %>

          </table>
        </div><!-- /.box-body -->
      </div><!-- /.box -->
    </div>
  </div>
<!-- @categories -->
</section>